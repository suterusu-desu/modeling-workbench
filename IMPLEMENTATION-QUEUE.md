# Remaining system integration

The Builder owns implementation and verified package adoption; the Planner supplies method requirements and assesses actual use. The Modeler owns live Blender qualification. Select a bounded slice when its trigger is observed, retain its counterexamples and compare actual operator results. A source audit disposition is not a delivered capability. Private evidence and source-study details remain in the originating workspace.

| Slice | Current disposition and trigger | Acceptance evidence |
| --- | --- | --- |
| Offline diagnostic recipes | Implemented in 0.2.4; historical operator use verified in a private workspace. 0.2.5 exposes compact step states and closes the interrupted-before-lease closure gap. | Synthetic tests cover pinning/recovery; actual use reproduced saved diagnostics, reused results across processes, selectively invalidated a changed branch and required renewed review. A fresh 0.2.5 inspection verified the direct summary route and conservative source-change invalidation without rerunning analysis or altering the store. This does not prove cross-character modeling quality. |
| Exact topology lineage | Implemented in 0.2.9: pinned operation declarations, composition and conservative forward/reverse selector remapping. | Installed synthetic operator use withholds a partial-merge selector; regressions cover splits, merges, deletion and introduced ancestry. Native producer adoption is explicitly deferred: the current study does not change topology and no verified producer exists to emit actual element ancestry. Do not fabricate it from proximity. |
| Target-domain and transition coverage | Implemented and used on actual saved cases in 0.2.6. 0.2.7 fixes completion metadata and routes only unresolved region-review suggestions. | Actual use distinguishes geometric candidates, fit, mask, response rows, numeric labels and supported region correspondence; it inspects named transition families independently. Functional diagnostic adoption is separate from completion-file repair and native quality. No automatic mask relaxation or target admission. |
| Surface correspondence evidence | Implemented and independently used through a separate 0.2.8 install: ordered alternatives, exact source-edge gaps and selected-section connectivity. | All 199 installed tests pass. Actual Builder use and independent Planner public bounded reads retain the saved gaps and alternatives without rerunning analysis or changing stored evidence. Planar disconnection is not full3D disconnection; anatomy, continuous correspondence and native admission remain unproved. See DESIGN.md. |
| Bounded native batches | Deferred for a specific unmet trigger: the current six isolated source inspections already use one invocation per immutable input. No repeated public native transport sequence or measured transport overhead was found. | The owner will identify an actual repeated sequence before selecting a closed batch contract. Repeated NPZ decompression is local analysis overhead, addressed by caching arrays once; a new transport executor would not fix it. Recipe journaling is not atomic native batching. |
| Lazy component catalogs | Implemented and used through a separate 0.2.9 install on a saved 55,765-triangle guide. | Shared-edge versus shared-vertex semantics, exact state/hash, compact metrics and lazy selection. Actual use locates two queried triangles in one 33,579-triangle component among 61 components with a 7,729-character catalog result and a bounded member page. Whole-mesh connectivity does not qualify a selected skin route. |
| Boundary ambiguity | Implemented in 0.2.9 as a bounded diagnostic over an explicitly supplied graph and independent node/edge masks. | Actual saved query finds an 18-hop ambiguous shortest route with a nonmanifold step; a small-budget query stays incomplete. Synthetic counterexamples retain exclusions, equal alternatives and unknown coverage. Hidden geometry or anatomical boundaries outside the declared graph remain unproved. |
| Save and rollback ordering | Existing scoped checkpoints and recovery remain available. The additional collaborative user-save/queued-rollback boundary is explicitly deferred, not claimed equivalent. | It needs a native-owner disposable-file test that deliberately inserts a user save/edit across a pending rollback. Current source inspection and guide construction provide no such transaction; this Builder slice has no native ownership and cannot establish that behavior through offline receipts. Original checkpoint and partial-effect evidence remain required. |
| Operator receipt and worker contracts | Implemented in 0.2.9 and used on original saved operation and worker receipts. Standalone completion helper is available to the owner without a live runtime switch. | Actual compact applied_trial result expands to consistent candidate bytes under its declared adapter contract. Original exit-zero/missing-inventory run remains failed/incomplete despite later independent verification. Synthetic helper use and regressions cover mandatory preflight, protected sources, missing outputs, conflicting and concurrent completion. The native owner integrated the exact helper into its next necessary isolated worker, which failed before completion; protected input hashes match. Successful native completion remains unverified and the owner handles the observed process failure. |
| Library dependency closure | Deferred because the current isolated source imports do not require merging a shared native library into a populated destination. | Existing explicit asset manifests plus separate protected-source hashes retain source identity. A new importer requires a concrete destination collision/shared-data closure case and preservation test; no blanket driver/constraint ban or speculative native importer is added. |
| UV, weights, rigging and animation | Excluded from this accepted implementation slice: the source locations were indexed, not validated as selected native operation contracts. | Current source-avatar research asks how actual controls move rim, fold, corners, ocular and lash layers. Native writer requirements must follow those findings; a broad rigging or animation API would invent a solution before mechanism evidence. |
| Rendering, look development and scene composition | Excluded from this slice: no specific accepted workflow failure or operation contract was established by the bounded audit. | Existing matched captures remain available. Broader scene/render features require a concrete observed need and source review before implementation. |
| Provider recipes and cost previews | Deferred because delivered recipes execute offline diagnostics only; the active provider route uses source-bound Studio preflight and reviewed intermediate inputs. | No executable provider graph exists to quote, and future dependent source/settings/costs are unknown. Keep exact per-job cost/currency and authorization in claim/reconcile. A composed preview requires a real authorized chain and must retain unknown prices; no API route, speculative prices or paid execution is introduced. |
| Cross-character portability | Workspace initializer and generic package delivered; equal modeling quality remains unproved. | Another character with explicit native/semantic bindings, preserved method and independently reviewed results; retain general gains and character-specific exceptions separately. |

The 0.2.7 operational follow-up repairs valid-destination temporary-path inflation and reconstructs missing completion markers from original durable results through public reconciliation. Original error-bearing receipts remain historical evidence; unknown effects stay unresolved. All five affected original calls were repaired and independently verified after installed regressions and copied-record tests; original result bytes and analysis counts were unchanged.

Reuse existing content records, episodes, bounded reads and operation recovery in each slice. Add abstractions when they expose a missing causal relation or reduce measured operator cost, rather than creating a second system of authority or execution.

Version 0.2.9 completes the selected offline mechanisms through a separately installed package: source and installed suites each run 258 tests successfully with one Windows symlink-privilege skip. Fresh launch matches the installed source and schema. Builder use exercises saved graph, catalog and original receipt cases plus explicitly synthetic lineage and worker contracts; Independent Planner review is pending because the user paused that task; reminders are on hold. Native owner helper use was attempted in the next needed worker, but that process failed before completion. These outcomes remain separate from successful installed offline and synthetic use. No active cache or native-owner binding is switched by this delivery.

Source-construction questions retained for the next supported method: distinguish primary movement from support/bulge, gaze and correctives; trace rim/fold/return paths, corners/contact and neighboring ocular/lash layers through actual control-to-surface influence. A narrowly observed numerical conditioning or array-access improvement needs its exact problem, unchanged constraints, residuals and cost comparison. It does not qualify anatomy or appearance and is not justification for a new native batch language.
