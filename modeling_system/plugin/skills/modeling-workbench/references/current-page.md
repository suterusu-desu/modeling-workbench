# Current page

Everything an agent needs before the next edit, and nothing else. Keep it under 10 KB. Update entries in place; move
history to `DECISIONS.md` (the owner's choices) and `LESSONS.md` (what was learned). A new workspace gets this page as
`PROJECT.md`.

## Character

- Character:
- Accepted neutral (identity guide): file, registered object, when and how the owner accepted it
- Reference material: where all of it lives (every image generation carries all of it)
- Units and frame:

## Guides

| Guide | Role (identity / pose_check / detail) | Source (images, job) | Registered file | Owner review |
|---|---|---|---|---|
| | | | | |

A pose guide fits a mechanism's parameters and checks it by overlap and volume; it is not a per-point target.

## Mechanisms

One card per moving feature, filled in from the reference avatars before building it.

- Feature:
- What moves (the band, how far it reaches):
- About what (hinge axis, slide direction):
- What stays still (corners, the opposing side, the socket):
- Timing (one law; how the controls or blend shapes carry it):
- Where it closes or lands:
- What it carries (lashes, markings):
- Which avatar shows it (study numbers):
- Declaration for the standard checks:
- Construction script and packaged calls it uses:

## Last owner-approved state

- File (path and hash):
- Motion video watched by the owner:
- The owner's words:

## Working state

- Live file and native owner:
- Current candidate (not approved):
- Known defects, largest first:

## Open owner decisions

-

## Next action

-

## Native binding

- Adapter (reference adapter or private), bridge port, restore hook:
- Blender executable and isolated profile:
